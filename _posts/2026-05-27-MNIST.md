---
layout: post
title: 'MNIST手写数字识别'
subtitle: '从零开始机器学习'
date: 2026-05-27 8:00:00 +0800 
author: 松鼠探长
cover: 'https://www.kaggle.com/competitions/3004/images/header'
cover_author: 'kaggle'
cover_author_link: 'https://www.kaggle.com/competitions/digit-recognizer/overview'
tags: 
- MNIST
- CV
- 计算机视觉
- 机器学习
- 神经网络
---
本项目为 上海大学 “人工智能基础A” 课程作业。

[数据集: https://www.kaggle.com/competitions/digit-recognizer/overview](https://www.kaggle.com/competitions/digit-recognizer/overview)

[视频讲解: https://www.bilibili.com/video/BV1bx411M7Zx](https://www.bilibili.com/video/BV1bx411M7Zx)

# 项目背景  
&emsp;&emsp;28x28 像素的灰度图像，代表一个手写数字，像素坐标为 0-783 ，每个坐标对应一个灰度值 0-255.

&emsp;&emsp;提供两个 csv 文件，分别为 test.csv（测试集，不含标签）与 train.csv（训练集，包含标签）  
&emsp;&emsp;测试集有 28000 个数据点，训练集有 42000 个数据点。

# 问题分析 #

## 模板法 

&emsp;&emsp;我们的目标是从含有标签的数据中推断出 0-9 这 10 种数字对应的规律。

&emsp;&emsp;容易想到 `模板法` ，找到每类数字每个像素对应的平均灰度，与样例作差求平方和，偏差的最小值即为最有可能的数字。这个方法的优点是解释简单，比较逻辑轻松，只需要最基本的 Numpy 与 Pandas 库即可实现。

&emsp;&emsp;模板法还可以这样优化，我们可以考虑引入“重心”，将字体全部平移居中，消除手写数字可能存在的相对位置偏差。

&emsp;&emsp;完整代码如下：

```Python
import numpy as np
import pandas as pd


# 定义reshape函数，调整字体位置
def reshape(pixels_array):
    N = pixels_array.shape[0]
    images = pixels_array.reshape(-1, 28, 28)
    aligned_images = np.zeros_like(images)

    # 预先准备好 0 到 27 的一维坐标数组
    coords = np.arange(28)

    for i in range(N):
        img = images[i]
        total_mass = np.sum(img)

        if total_mass == 0:
            aligned_images[i] = img
            continue

        # 沿列求和，得到每行的质量分布，从而计算“行重心”（垂直方向 y）
        row_masses = np.sum(img, axis=1)
        centroid_y = np.sum(coords * row_masses) / total_mass

        # 沿行求和，得到每列的质量分布，从而计算“列重心”（水平方向 x）
        col_masses = np.sum(img, axis=0)
        centroid_x = np.sum(coords * col_masses) / total_mass

        # 计算偏离中心的距离
        shift_y = int(round(13.5 - centroid_y))
        shift_x = int(round(13.5 - centroid_x))

        # 平移：axis=0是垂直，axis=1是水平
        shifted_img = np.roll(img, shift_y, axis=0)
        shifted_img = np.roll(shifted_img, shift_x, axis=1)

        aligned_images[i] = shifted_img

    return aligned_images.reshape(N, 784)


# 1. 读取并划分数据集
raw_data = pd.read_csv("train.csv")

# 验证集：前 10000 行
val_data = raw_data.iloc[:10000]
# 训练集：10000 行之后的所有数据
train_data = raw_data.iloc[10000:]

# 提取训练集的标签和像素
train_labels = train_data.iloc[:, 0].values
raw_train_pixels = train_data.iloc[:, 1:].values
train_pixels = reshape(raw_train_pixels)

# 提取验证集的标签和像素
val_labels = val_data.iloc[:, 0].values
raw_val_pixels = val_data.iloc[:, 1:].values
val_pixels = reshape(raw_val_pixels)

print(
    f"数据集划分完成：训练集 {len(train_pixels)} 行，验证集 {len(val_pixels)} 行。"
)


# 2. 构建均值模板

templates = np.zeros((10, 784))  # 归零化
feature_dim = train_pixels.shape[1]  # 获取新的特征维度

for digit in range(10):
    digit_mask = (
        train_labels == digit
    )  # 布尔判断筛选目标标签图像，返回掩码
    templates[digit] = np.mean(
        train_pixels[digit_mask], axis=0
    )  # 按行计算平均模板

# 3. 验证并计算准确率

correct_predictions = 0

for i, val_sample in enumerate(val_pixels):
    # 计算当前验证样本与 10 个均值模板的平方和
    squared_diff = (templates - val_sample) ** 2
    sum_of_squares = np.sum(squared_diff, axis=1)

    # 预测数字
    predicted_digit = np.argmin(sum_of_squares)

    # 与真实标签进行比对
    if predicted_digit == val_labels[i]:
        correct_predictions += 1

# 计算准确率
accuracy = (correct_predictions / len(val_pixels)) * 100
print(
    f"验证集测试完成！准确率为: {accuracy:.2f}% ({correct_predictions}/{len(val_pixels)})"
)
```
&emsp;&emsp;模板法经过测试，前 10000 行验证的准确率为 82.12% ，说明有效，但还有很大优化空间。

&emsp;&emsp;那么具体该怎么优化呢？

## 加权法

> 小步快跑，试错迭代。  &emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;——马化腾



&emsp;&emsp;不难看到模板法的局限性。这种方法将所有 784 个像素都给予了相同的权重。而实际上，对于一张手写数字图片，未必所有像素都得一样重要。例如，对于数字 0 而言，假如图片中间出现了一块很亮的像素，则几乎可以直接排除了。

&emsp;&emsp;所以，我们可以对每一类数字，整理其 784 个坐标的权重。如果某个像素与这个数字具有一定的正相关性，那就给予一个正权重，反之则为负权重。如此，对于单张图片，我们可以给出一个 10x784 的权重矩阵 W ，再左乘代表灰度的 784x1 的矩阵 X ，得到一个加权和构成的行向量 Z。

![](/assets/img/Z.svg)

&emsp;&emsp;那么这里的权重W该如何确定呢？总不能手工一个个标记吧。有没有一种算法，能够帮我们确定这个合适的权重呢？

&emsp;&emsp;答案是肯定的。但必须指出，这里的算法可能并不一定像计算平均模板那样，简洁直观，一步到位地 **确定** 合适的权重，而往往是以一种可重复执行的步骤，去 **逼近** 这个合适的权重。毕竟我们有几万条数据，而大数定律告诉我们，要想算出来几乎相同的模板，几千条数据就已经足够了，那更多的那些数据，就为这样可重复执行的步骤创造了条件，而重复执行，逐渐逼近，也是机器学习的核心思想之一。

&emsp;&emsp;现在，我们的行动方案已然确定——找到一种可重复执行的步骤，通过这几万条数据，让机器去 **预测** 每类数字所对应的坐标权重。它的起点可以很粗糙，但应当能够根据预测的成功与否，倒逼机器动态调整参数，最后收敛到一个稳定的值附近。

&emsp;&emsp;那么进一步拆解目标，就变成了

&emsp;&emsp;**1.找到一种方法来刻画机器预测的结果是 “好” 还是 “不好” ，并反馈给机器。**

&emsp;&emsp;**2.让机器去分锅，找到出问题的参数，并调整。**

&emsp;&emsp;如此，不断往复，直到训练完这 4 万多个数据集。可以设想，经过大量的训练，机器对每个参数的预测将会越来越准确。

### 激活函数与损失函数

&emsp;&emsp;想要刻画机器预测的结果，首先要统一机器预测的结果。单纯的矩阵乘积拿出来效果并不佳，因为结果的上下限与符号不确定，难以系统比较。

&emsp;&emsp;我们想起了概率论，让机器输出一个项数为 10，和为 1的概率数组，如\[0.1,0.2,...,0.2,0.3\]，描述对于这张图片，分别是数字 0/1/2...9 的把握是多少。

&emsp;&emsp;那么，我们如何将无界的加权和，映射到有界的概率呢？由此我们引入了`激活函数Softmax`，它有着一系列良好的性质，能将无界的输出值 Z 映射到 0-1 的概率分布，且保留原来的单调性。最关键的是，Softmax 的导数非常好求，意味着我们可以方便地对它进行其他处理。

&emsp;&emsp;Softmax 的公式为
![](/assets/img/softmax.svg)

&emsp;&emsp;有了归一化的结果，就可以方便地与标签做比较，这里我们引入`损失函数Loss`，刻画机器预测与实际结果的偏离值。损失函数并不是线性的，而是一个对数函数，为的是放大极端值造成的影响，公式如下：
![](/assets/img/loss.svg)

其中y代表真实标签，是一个非 0 即 1 的 10 维行向量：
![假如这个图片对应的真实数字为2，y代表的行向量](/assets/img/y.svg)

### 反向传播与梯度下降

&emsp;&emsp;显然，机器如果想要降低错误，就必须要将损失函数降到最低。要想降低损失函数，就必须改变权重矩阵 W 。而这里有一个传播过程，从 L 通过损失函数传到概率 P ，再通过激活函数传到加权和 Z ，最后到达权重矩阵 W ，这个过程就是`反向传播`
![反向传播示意图](/assets/img/bp.svg)

&emsp;&emsp;在整个过程中，我们关心的是，对于权重矩阵的某个改变量 dW ，整个 L 最后的改变量是多少。你一定会感到特别熟悉，因为这就是大家都学习过的偏导数。根据链式法则，容易求出 L 关于 W 的偏导数:

![](/assets/img/bp2.svg)

&emsp;&emsp;得到了这个公式，我们就可以理解损失函数 L 与 权重矩阵 W 的关系了。由于灰度矩阵 X 非负，要想让损失函数越小（模型越正确），W 必须沿着`梯度下降`的方向更新:
![这就是大名鼎鼎的“梯度下降”](/assets/img/gc.svg)

&emsp;&emsp;在梯度前我们引入了一个系数 η ，此为学习率，描述参数沿梯度方向移动的距离，用于控制参数更新的步长。如果学习率太低，步子迈得太小，整个模型会收敛得很慢，训练时间长，也可能陷入局部最优。但如果学习率过高，参数可能会来回震荡，甚至发散。因而精确控制学习率尤为关键。

&emsp;&emsp;让我们来理解一下这个公式。假如某张图片的标签是 2 ，且模型对于该图片为 2 的预测概率为 P2 ，模型预测正确，P2应该越大越好，那么就有
![对正确类别的预测，权重增大](/assets/img/gc2.svg)

&emsp;&emsp;由于概率不大于 1 ，整个梯度小于等于 0 ，参数会向增大的方向移动，也就是提高该像素位置的权重，模型就会更“自信”地提高该位置像素与标签 2 的正相关性。

&emsp;&emsp;还是这张图片，正确答案为 2，模型对于该图片为 3 的预测概率为 P3 ，显然这个预测不太对，P3不应该太大，那么就有 
![对错误类别的预测，权重减小](/assets/img/gc3.svg)

&emsp;&emsp;由于概率不小于 0 ，整个梯度大于等于 0 ，参数会向减小的方向移动，也就是降低该像素位置的权重，模型会降低该位置像素于标签 3 的正相关性，确实有效地符合了我们对这个问题的要求。

&emsp;&emsp;现在，我们已经跑通了整个流程：计算加权→激活函数→预测概率→损失函数→反向传播→梯度下降→权重更新，使用 Numpy 库与 Pandas 库即可完成，具体实现代码如下：

```Python
import numpy as np

def softmax(Z):
    # 激活函数
    # 减去最大值是为了防止指数运算溢出（NumPy 的数值稳定性技巧）
    exp_Z = np.exp(Z - np.max(Z, axis=1, keepdims=True))
    return exp_Z / np.sum(exp_Z, axis=1, keepdims=True)


def compute_loss(probs, y_true):
    # 损失函数：交叉熵损失
    batch_size = probs.shape[0]
    # 提取出正确类别的预测概率，计算交叉熵损失
    correct_log_probs = -np.log(probs[range(batch_size), y_true] + 1e-15)
    return np.sum(correct_log_probs) / batch_size

import pandas as pd

# 1. 读取并划分数据

print("正在读取原始训练数据...")
raw_data = pd.read_csv("train.csv")

val_data = raw_data.iloc[:2000]
train_data = raw_data.iloc[2000:]

# 提取特征并归一化像素值 0~1 
train_labels = train_data.iloc[:, 0].values
train_pixels = train_data.iloc[:, 1:].values / 255.0

val_labels = val_data.iloc[:, 0].values
val_pixels = val_data.iloc[:, 1:].values / 255.0

print(f"数据准备完毕。训练集: {train_pixels.shape}, 验证集: {val_pixels.shape}")


# 2. 初始化权重

# W 的形状是 (784, 10)，代表 784 个像素坐标对应 10 个数字的权重
W = np.random.randn(784, 10) * 0.01
# b 的形状是 (1, 10)，代表 10 个数字的基础分（偏置）
b = np.zeros((1, 10))

# 超参数设置
learning_rate = 0.1  # 学习率
batch_size = 64  # 批大小：每次抓取 64 张图
epochs = 5  # 重复训练 5 遍

# 3. 开始训练

num_samples = train_pixels.shape[0]

for epoch in range(epochs):
    print(f"\n--- 开始第 {epoch + 1} 轮训练 ---")

    # 打乱训练数据
    permutation = np.random.permutation(num_samples)
    X_shuffled = train_pixels[permutation]
    y_shuffled = train_labels[permutation]

    # 按批次（Batch）进行训练
    for i in range(0, num_samples, batch_size):
        X_batch = X_shuffled[i : i + batch_size]
        y_batch = y_shuffled[i : i + batch_size]
        current_batch_size = X_batch.shape[0]

        # ---- 步骤 A: 前向传播 
        
        # 矩阵点积：(64, 784) 乘 (784, 10) -> (64, 10) 的得分矩阵
        Z = np.dot(X_batch, W) + b
        # 调用激活函数计算概率
        probs = softmax(Z)

        # ---- 步骤 B: 反向传播
        # 处理真实标签
        y_one_hot = np.zeros_like(probs)
        y_one_hot[range(current_batch_size), y_batch] = 1.0

        # 梯度
        dZ = probs - y_one_hot

        # 分摊权重
        # (784, 64) 点积 (64, 10) -> (784, 10) 的权重更新方向
        dW = np.dot(X_batch.T, dZ) / current_batch_size
        db = np.sum(dZ, axis=0, keepdims=True) / current_batch_size

        # ---- 步骤 C: 梯度下降
        W = W - learning_rate * dW
        b = b - learning_rate * db

    
    # 4. 每一轮结束，在验证集上检验成果
  
    val_Z = np.dot(val_pixels, W) + b
    val_probs = softmax(val_Z)
    val_predictions = np.argmax(val_probs, axis=1)

    # 计算准确率
    accuracy = np.mean(val_predictions == val_labels) * 100
    loss = compute_loss(val_probs, val_labels)
    print(f"第 {epoch + 1} 轮完成 -> 验证集损失: {loss:.4f} | 准确率: {accuracy:.2f}%")
```

&emsp;&emsp;在学习率为 0.1 ，重复训练次数为 5 的条件下，最终的训练准确率稳定在 92% 附近，相比模板法提升了 10% ，是非常可观的进步。

&emsp;&emsp;值得注意的是，除了用准确率来衡量模型的结果，我们还要求模型给出`验证集损失`来描述它对这个结果的“把握程度”。毕竟 0.51 对 0.49 ，在最后结果上也是“正确”的，但多少有些歪打正着。我们利用损失函数会客观地记录下每个预测对实际标签的偏差的性质，要求机器将验证集上的损失反馈给我们，就能更准确地评估模型的性能了。

## 神经网络

> Even when it works, dig into why. &emsp;&emsp;即便可行，亦探真理。

&emsp;&emsp;现在，90% 以上的准确率已经能基本满足大部分课程作业的要求。但我们的追求肯定不止于此。你肯定会很好奇，是否存在一种方法，能够做到接近 100% 的准确率，就像人类一样几乎不会出错？

&emsp;&emsp;这种方法当然存在，就是`神经网络`

&emsp;&emsp;果然，要想变得和人一样优秀，还得从人自己身上取经。

&emsp;&emsp;简单复习一下刚刚的加权法，我们通过权重矩阵 W 与灰度矩阵 X 的乘积描述了一个数字的“可能性”。这种算法固然不错，但总感觉不太符合我们判断数字的 ***直觉*** 。数字总感觉不太应该拆成一个个像素小点，而更应该看作是一系列笔画特征的组合。例如 1 是一竖， 7 是一个短横线加上一根长斜线，8 是两个小圆圈上下叠放......

&emsp;&emsp;我们不禁会想，那如果我们做两遍加权和，第一遍对各个像素的灰度做加权，用来描述横、圈、竖线这种笔画特征，第二遍用这些笔画特征做加权，描述这 10 种不同的数字，会不会更加精细，更为准确呢？

&emsp;&emsp;这个想法非常巧妙，它非常贴近人脑的生理构造！人脑有近千亿神经元，每个神经元间通过树突/轴突链接，构建起一个巨大规模的神经网络。各个神经元相当于不同的小分类器，接受不同领域的信号，并通过神经网络向其它神经元传递。整个传播的过程是有条件的：需要输入信号（加权和）达到一定的阈值后才会触发传播。从宏观上来看，神经网络将复杂的目标层层拆解，最终通过大量基础元素的组合来实现对世界的认知。

&emsp;&emsp;其实我们刚刚使用过的“加权法”，已经是一个单层的神经网络了，不过它是在一个 784 维的空间中，用 10 条笔直的线（10 类数字的加权和）去划分。可想而知，不同类型数字的分界线未必是分明的，更是难以用一条线去简单切分的。如果我们构造一种稍微复杂的函数，能够不那么笔直，在边界处“弯曲”一些，想来肯定会更加正确。

&emsp;&emsp;要想构造一种非线性的函数，至少需要存在一层内函数是非线性的。也就是说，我们不能只做加权和，因为有限次线性变换总能被一次线性变换所表示。因此不妨考虑只有一层非线性 + 一层线性的情况：让第一层通过某种非线性的方式激活后，指向圆圈，短横，斜线这种笔画样式；第二层接收第一层的输出，再通过加权和，反映各种笔画样式的组合，最终激活成 0 1 2 3 这些数字的概率，和之前的方法如出一辙。

&emsp;&emsp;现在困扰我们的头号难题变成了：这个非线性的激活函数，到底怎么选取？

&emsp;&emsp;大道至简，我们继续借鉴人脑的工作流程。神经元输入信号需要达到一定阈值才会继续传播，我们不妨让这个函数具有同样的性质：对于那些与笔画样式负相关（负权重）的输入信号，直接抹除，正相关的输入信号则予以保留。这个函数就是著名的 ReLU 激活函数，公式很简单：

![](/assets/img/ReLU.svg)

&emsp;&emsp;从宏观上来看，这个激活函数做了一件聚焦的事，只关心那些对于某笔画样式具有正相关的像素。对于那些无相关或者不相关的部分一概忽略。因此，通过 ReLU，我们可以构建极高效率的分类器，也就是神经网络的第一层。

&emsp;&emsp;同时 ReLU 的性质十分良好，对于正相关的部分会原封不同地复制出来，不必担心其它激活函数上可能出现的数值波动问题。同时它在小于等于 0 时的导数为 0 （0 处虽然不可导但也可看作为 0），这意味着在反向传播的“分锅”过程中，那些零贡献的灰度值所构成的梯度也会被自然忽略，大大节省了运算开销。

&emsp;&emsp;然而，多层神经网络也存在不可避免的问题，就是`算法黑箱`。在实际训练过程中，我们再也无法像单层神经网络那样，明确得知模型对每一个像素坐标灰度的加权情况。换而言之，在多层神经网络中，中间的若干层不再直接面对输入与输出，而是与其它中间层耦合在一起，牵一发而动全身。因而这些不直接连接原始输入层与输出层的部分，也被称为`隐含层`。尽管我们知道这些隐含层代表着不同的笔画样式，但机器可不会按照常理出牌。在它们眼中，“短横”说不定被看作是多个“短竖”的叠加呢。

&emsp;&emsp;既然无从干预过程，我们就只能关注结果。然而结果正义往往并不等于程序正义。`算法黑箱`的不可解性，将成为人工智能发展上的永恒谜题。

&emsp;&emsp;现在，我们引入了新的非线性激活函数ReLU，构造多个隐含层提取高维特征，其余损失函数与验证部分照旧，完整代码如下：
```Python
# 基于Python 3.13.9和PyTorch 2.7.1的双隐藏层神经网络
import torch
import torch.nn as nn
import torch.optim as optim
from torch.utils.data import DataLoader, Dataset
import pandas as pd
import numpy as np

# 1. 基础参数设置

BATCH_SIZE = 64          # 每个批次处理 64 张图片
EPOCHS = 10              # 重复使用训练集10遍
LEARNING_RATE = 0.1     # 学习率
DEVICE = torch.device("cuda" if torch.cuda.is_available() else "cpu") #利用GPU加速


# 2. 载入数据集

class LocalMNISTDataset(Dataset):
    """用于读取本地 CSV 格式 MNIST 数据集的自定义类，也可使用dataset官方MNIST数据集"""
    def __init__(self, pixels, labels):
        # 将像素数据归一化到 0~1，并转换为 PyTorch 张量
        self.pixels = torch.tensor(pixels / 255.0, dtype=torch.float32)
        self.labels = torch.tensor(labels, dtype=torch.long)
        
    def __len__(self):
        return len(self.labels)
        
    def __getitem__(self, idx):

        return self.pixels[idx], self.labels[idx]

print("正在读取本地 train.csv 数据...")
raw_data = pd.read_csv("train.csv")

# 切分训练集与验证集
val_df = raw_data.iloc[:2000]
train_df = raw_data.iloc[2000:]

# 提取特征并分离标签
train_labels = train_df.iloc[:, 0].values
train_pixels = train_df.iloc[:, 1:].values

val_labels = val_df.iloc[:, 0].values
val_pixels = val_df.iloc[:, 1:].values

# 构建 PyTorch 专属的数据集实体
train_dataset = LocalMNISTDataset(train_pixels, train_labels)
test_dataset = LocalMNISTDataset(val_pixels, val_labels)

train_loader = DataLoader(dataset=train_dataset, batch_size=BATCH_SIZE, shuffle=True)
test_loader = DataLoader(dataset=test_dataset, batch_size=BATCH_SIZE, shuffle=False)

print(f"本地数据准备完毕。训练集: {train_pixels.shape[0]} 条，验证集: {val_pixels.shape[0]} 条。")


# 3. 双隐藏层神经网络

class SimpleMLP(nn.Module):
    def __init__(self):
        super(SimpleMLP, self).__init__()
        
        self.fc1 = nn.Linear(784, 512)  # 第一层：把 784 个原始像素映射到 512 个基础特征
        self.relu1 = nn.ReLU()          # 激活函数ReLU
        
        self.fc2 = nn.Linear(512, 256)  # 第二层：把 512 个基础特征组合成 256 个更高阶的特征
        self.relu2 = nn.ReLU()          # 激活函数ReLU
        
        self.fc3 = nn.Linear(256, 10)   # 第三层：最终决策，结算出 10 个数字各自的得分
        
    def forward(self, x):
        
        # 1. 经历第一次加权和截断
        x = self.fc1(x)
        x = self.relu1(x)
        
        # 2. 经历第二次加权和截断
        x = self.fc2(x)
        x = self.relu2(x)
        
        # 3. 最终输出原始得分（PyTorch 自动接续 Softmax）
        return self.fc3(x)

# 实例化模型
model = SimpleMLP().to(DEVICE)

# 4. 损失函数与梯度下降
criterion = nn.CrossEntropyLoss()  # 交叉熵损失函数
optimizer = optim.SGD(model.parameters(), lr=LEARNING_RATE)  # 梯度下降

# 5. 训练与验证循环

if __name__ == '__main__':
    print(f"当前运行设备: {DEVICE}")
    print("开始训练多层感知机（MLP）...\n")
    
    for epoch in range(EPOCHS):
        # --- 训练阶段 ---
        model.train()
        running_loss = 0.0
        for data, target in train_loader:
            data, target = data.to(DEVICE), target.to(DEVICE)
            
            optimizer.zero_grad()             # 1. 抹除残余梯度
            outputs = model(data)             # 2. 计算得分
            loss = criterion(outputs, target) # 3. 计算本批次的交叉熵损失
            loss.backward()                   # 4. 反向传播，利用链式法则层层回传梯度
            optimizer.step()                  # 5. 梯度下降，更新这一层层的所有权重
            
            running_loss += loss.item()
            
        avg_train_loss = running_loss / len(train_loader)
        print(f"Epoch [{epoch+1}/{EPOCHS}] - 训练集损失率: {avg_train_loss:.4f}")
        
        # --- 验证阶段 ---
        model.eval()
        val_loss = 0.0  # 用来累加验证集总损失的记账本
        correct = 0
        
        with torch.no_grad(): 
            for data, target in test_loader:
                data, target = data.to(DEVICE), target.to(DEVICE)
                outputs = model(data)
                
                # 验证集损失
                loss = criterion(outputs, target)
                val_loss += loss.item()
                
                # 输出预测结果
                pred = outputs.argmax(dim=1, keepdim=True)
                correct += pred.eq(target.view_as(pred)).sum().item()
                
        avg_val_loss = val_loss / len(test_loader)  # 计算验证集平均损失率
        accuracy = 100. * correct / len(test_loader.dataset)
        
        print(f"- 验证集损失率: {avg_val_loss:.4f} | 准确率: {accuracy:.2f}%\n")
```

&emsp;&emsp;在学习率为 0.1 ，学习轮次为 10 的条件下，准确率来到了 97.5-98.0% 附近，且损失率均处于 0.1 以下，大获成功！