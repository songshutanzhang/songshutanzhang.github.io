---
layout: post
title: 'MNIST手写数字识别'
subtitle: '从零开始机器学习'
date: 2026-05-28 20:00:00 +0800 
author: 松鼠探长
cover: 'https://www.kaggle.com/competitions/3004/images/header'
cover_author: 'kaggle'
cover_author_link: 'https://www.kaggle.com/competitions/digit-recognizer/overview'
tags: 
- MNIST
- CV
- 计算机视觉
- 人工智能
- 机器学习
---
# MNIST手写数字识别 #
&emsp;&emsp;这是我们大一下“人工智能基础”课程的课程作业。

&emsp;&emsp;项目地址：https://www.kaggle.com/competitions/digit-recognizer/overview

## 项目背景 ## 
&emsp;&emsp;28x28像素的灰度图像，代表一个手写数字，像素坐标分别从0-783，每个坐标对应一个灰度值0-255.

&emsp;&emsp;提供两个csv文件，分别为test.csv（测试集，不含标签）与train.csv（训练集，包含标签）  
&emsp;&emsp;测试集有28000个数据点，训练集有42000个数据点。

## 问题分析 ##
&emsp;&emsp;我们的目标是从含有标签的数据中推断出0-9这10种数字对应的规律。

&emsp;&emsp;容易想到 `模板法` ，找到每类数字每个像素对应的平均灰度，与样例作差求平方和，最小值即为最有可能的数字。只需要最基本的 Numpy 与 Pandas 库即可实现。

```Python
#模板算法，平均值求差再平方和，只用numpy与pandas库
#在10000行验证集上的测试结果为81.47%
import numpy as np
import pandas as pd

# ==========================================
# 1. 读取并划分数据集
# ==========================================
print("正在读取原始训练数据...")
raw_data = pd.read_csv("train.csv")

# 验证集：前 10000 行
val_data = raw_data.iloc[:10000]
# 训练集：10000 行之后的所有数据
train_data = raw_data.iloc[10000:]

# 提取训练集的标签和像素
train_labels = train_data.iloc[:, 0].values
train_pixels = train_data.iloc[:, 1:].values

# 提取验证集的标签和像素
val_labels = val_data.iloc[:, 0].values
val_pixels = val_data.iloc[:, 1:].values

print(f"数据集划分完成：训练集 {len(train_pixels)} 行，验证集 {len(val_pixels)} 行。")

# ==========================================
# 2. 训练阶段：构建均值模板
# ==========================================
print("正在构建每个数字的均值模板...")
templates = np.zeros((10, 784)) #归零化

for digit in range(10):
    digit_mask = train_labels == digit #布尔判断筛选目标标签图像，返回掩码
    templates[digit] = np.mean(train_pixels[digit_mask], axis=0) #按行计算平均模板

print("均值模板构建完毕。")

# ==========================================
# 3. 验证阶段：在验证集上测试并计算准确率
# ==========================================
print("正在验证集上进行预测...")
correct_predictions = 0

for i, val_sample in enumerate(val_pixels):
    # 计算当前验证样本与 10 个均值模板的平方和
    squared_diff = (templates - val_sample) ** 2
    sum_of_squares = np.sum(squared_diff, axis=1)

    # 预测数字
    predicted_digit = np.argmin(sum_of_squares)

    # 与真实标签进行比对
    if predicted_digit == val_labels[i]:
        correct_predictions += 1

# 计算准确率
accuracy = (correct_predictions / len(val_pixels)) * 100
print(
    f"验证集测试完成！准确率为: {accuracy:.2f}% ({correct_predictions}/{len(val_pixels)})"
)
```